# design-skills-at-scale — decisions

<!-- Append-only: date — choice — why. -->

- 2026-08-20 — Ruling (controller, work-run): resolve the sibling checkout in every `node ../Agent-Engineering/scripts/agent-lint.mjs .` acceptance command to its real location on this machine, `/c/Briar/repos/mine/Agent-Engineering` — command semantics unchanged, only the path resolved, and the pre-existing `LOW cmd-drift` finding on `AGENTS.md:15` is therefore expected and not a lane defect — because this lane runs in a linked git worktree, so `../Agent-Engineering` does not resolve from the lane's repo root, and `agent-lint.mjs:25-31` already declares such an escaping path "correct wherever that path exists outside the repo … and absent elsewhere (a worktree)", reporting it `low` rather than failing.
- 2026-08-20 — Ruling (controller, work-run): the read-only evidence repo `<PEGASUZ>/Pegasuz-Core/frontend-admin/admin` (PLAN constraints block) resolves on this machine to `/c/Briar/repos/mine/../../repos/work/Pegasuz/Pegasuz-Core/frontend-admin/admin`, i.e. `/c/Briar/repos/work/Pegasuz/Pegasuz-Core/frontend-admin/admin` — verified present — and stays READ-ONLY for the whole lane, not one byte written there — because machine-anchored paths are permitted in `work/` (`agent-lint.mjs:354` scopes the `machine-path` check to `skills|reference|templates|global|loops` only) but never in `skills/`.
- 2026-08-20 — Ruling (controller, fix round 1): reshape this file's first two entries into the three-segment `- YYYY-MM-DD — <choice> — <why>` shape now, without losing any of their content, rather than deferring the reformat to work-verify — because PLAN step 4 appends at least three more rulings to this file, and reformatting later costs more than reformatting two entries now; this file's own header comment states that shape one line above, and `Agent-Engineering/templates/repo/work/DECISIONS.md.template:4` is the exact template this file was scaffolded from.
- 2026-08-20 — Ruling (controller, fix round 2, Ruling A): fix `feature_list.json` F07's unpasteable `verification` string alongside F08/F09/F11 in this round, rather than leaving it deferred — because F07's original deferral (round 1) covered a different defect, the reviewer's Minor that its second clause is a content judgment rather than a command; the pasteability break (prose spliced into command position) was never itself deferred, and moving that judgment-call note into `behavior` resolves the deferred Minor as a side effect instead of conflicting with it.
- 2026-08-20 — Noted (controller, fix round 2, Ruling C, deferred — do not act): the re-review's Minor that `DECISIONS.md`'s reshaped entries still carry an extra internal em-dash used as ordinary prose punctuation, so they don't parse as a clean three-field record, is genuine polish with content already intact; it is deferred to work-verify's triage and these lines are intentionally left unedited in this round.
- 2026-08-20 — Ruling (controller, work-run): PLAN steps 2 and 3 run their cold baseline in TWO seats — a cold runner subagent that is given only the current SKILL.md workflow and the read-only evidence repo, and is told nothing about this lane; then the step's own implementer, who reads the lane and transcribes that runner's report verbatim into the baseline file — because the implementer dispatch template requires reading SPEC.md, and SPEC §5 records three predictions on purpose so the baseline can contradict them; an implementer who had read them would be primed to reproduce them, which would make the transcript worthless as the evidence SPEC §5 makes binding. The controller dispatches the cold runner, never the implementer (implementers do not dispatch subagents).
- 2026-08-20 — Ruling (controller, work-run): the cold baseline is taken against the skills as installed at the user's global skills folder, verified byte-identical to this worktree's copies before the run — and every later proving run must point explicitly at this worktree's `skills/` copies instead — because the installer junction-links the global folder to the MAIN checkout, not to this worktree, so once steps 7-8 rewrite the skills here the global copies still resolve to the unmodified originals.
- 2026-08-20 — Ruling (controller, work-run): every review and re-review seat from step 2 onward delivers its verdict by WRITING it to a scratch file, replying only `DONE`, and the controller reads the file — because the step-2 fix-round re-review burned two seats (sonnet, then opus) that each ran and then went idle repeatedly with the report never reaching the controller, across five resend requests in two phrasings; no refusal was ever observed, the text simply did not arrive, and the third seat delivered intact on its first attempt once the delivery method changed.
- 2026-08-20 — Ruling (controller, work-run): PLAN step 4's stated acceptance, `test $(grep -c '^- 2026-' DECISIONS.md) -ge 3`, is kept as written but is NOT the step's real bar — the substantive bar is three dated rulings that each name one of SPEC §5's three predictions explicitly and verdict it confirmed / contradicted / unobserved, and the step reviewer checks that — because this file already carried more than three `- 2026-` entries before step 4 began, so the command as written cannot fail and would pass on an empty step; recording the weakness rather than silently relying on a gate that proves nothing.
- 2026-08-20 — Ruling (step 4, SPEC §5 prediction 1, "step 1 offers to instantiate a DESIGN.md"): CONTRADICTED on the literal wording, with the defect it was reaching for present in a more severe form; SPEC §3.5 amended with a dated step-4 note, approved text not rewritten — because in `baseline-designing-consistently.md` `## Step 1` the run searched exhaustively (six searches, zero hits), quoted the skill's instantiate fallback, and made no offer of any kind, and its `## What I would have committed` records only that "no `DESIGN.md` was created or edited"; the two causes the runner gave are de-confounded here, the step-2 preamble having handed this reconciliation to step 4 by name: the scope fence is not load-bearing, since controller addendum 4 verified that `Context-Engineering` exists nowhere on this machine, so a fence-free runner hits the same dead pointer, and READ-ONLY blocks creating a file rather than offering one, in a run that used its "what I would have written / committed" convention four times for other blocked writes (Step 3's component, Step 3's view diff, Step 4's would-be Decisions line, the closing section) and never once to record an offer, while the absence of a live owner means an offer could only ever have been recorded, and none was; the underlying defect is present and worse than predicted, since step 1 produced neither an offer nor a discovery but a dead end, and the standing system (the `--nl-*` line the admin's own `AGENTS.md` names, `nueva-linea.css`'s 288 lines of tokens, inline dated owner decisions carrying names and ticket IDs such as "dueño, 2026-08-20, PEG-60") surfaced only at `## Step 2`, flagged there by the runner as "the skill did not tell me to do this"; flagged for the controller and deliberately NOT acted on: SPEC §4 requirement 1's second clause, that step 1 "does not propose instantiating a DESIGN.md while a system is discoverable", would pass vacuously against today's unfixed skill, which never reaches a proposal at all, so an eval written only to that clause would not be a test.
- 2026-08-20 — Ruling (step 4, SPEC §5 prediction 2, "step 4 elects `13px` a token on frequency"): CONTRADICTED on the literal wording, with the defect it was reaching for present in a different place, the emitted frontmatter; SPEC §3.6 amended with a dated step-4 note, approved text not rewritten — because in `baseline-extracting-design-md.md` `## Step 4` the 92 `text-[13px]` uses were never elected: the run refused to elect any token for the 430 arbitrary-pixel type values, calling them "a genuine fork in the road", and carried them to `## Step 5` as an open question, while the one type token elected on frequency was `ui` (2644 against 801 and 138), a case where frequency and the codebase's own most recent source-of-truth comment agreed; 13px does reach the emitted frontmatter as `small: '0.8125rem'`, but as the doc47 named role (`text-small`, 398 uses) carried in wholesale with the rest of that declared config scale, not as the arbitrary drift value, so the predicted mechanism did not operate; the defect is nonetheless present one layer down and sharper than predicted, because the frontmatter ships elections the runner itself judged unsafe as unqualified law: `radius: card: '14px'` elected "per the rule as stated" at 984 against roughly 751 while flagged as "exactly the kind of near-tie the skill would want an owner's eyes on", and `radius: sheet: '20px'` shipping although `## Step 4` states "I did **not** elect a token for this role" and its 12px competitor leads it 68 to 12, with eleven of the twelve `type:` roles shipping with no individual election work at all and the `### Open` prose under `## Decisions` carrying doubt that the machine-readable half of the file cannot express; no lane fence touches this evidence, the step-3 preamble recording that NO-SUBAGENTS does not touch Step 4's token-election reasoning and that READ-ONLY touches neither the harvest nor the elections, and the one fence-adjacent gap, no live owner to confirm with, strengthens rather than weakens the finding, since an absent owner is precisely the condition under which the rule as written still emitted a complete token block; flagged for the controller and deliberately NOT acted on: SPEC §4 requirement 5 ends "every unconfirmed token ships `[provisional]`" while SPEC §3.1 defines that marker only inside `## Decisions` entry text, so a frontmatter token has no defined way to carry it, and requirement 5 is left exactly as approved.
- 2026-08-20 — Ruling (step 4, SPEC §5 prediction 3, "neither produces a bounded slice"): CONFIRMED for `extracting-design-md` on direct evidence and UNOBSERVED for `designing-consistently`, which never exercised the behavior; no SPEC §3 amendment, since the design's prescriptions are not what the evidence contradicts — because in `baseline-extracting-design-md.md` `## Step 5` the instruction "Read each surface" met 157 view files, the runner did not read them, substituted a bound of its own (one structural pattern checked across the 15 `*DetailView.vue` files) and disclosed it as "a deliberate, bounded substitution for 'read each surface'", i.e. the skill supplied no bounding rule and the runner had to invent one off script; whereas in `baseline-designing-consistently.md` `## Step 2` there was no `DESIGN.md` and therefore no `## Decisions` to slice, "not 'few entries,' literally none", so the slicing behavior was never exercised and could only have been observed had the repo contained a `DESIGN.md` with a populated `## Decisions` section, an absence that is fence-independent (the step-2 preamble lists it among the absence facts, and the step-3 runner independently confirmed "no existing DESIGN.md anywhere"), and a negative compound prediction passes vacuously on that half, which is not evidence, so it is recorded unobserved rather than folded into the confirmation; the fence entanglement is resolved rather than assumed: NO-SUBAGENTS did block the skill's own `## Scaling (bounded)` fan-out lever at roughly ten times its stated ~15-surface threshold and `## Step 5` opens "Same scale problem as Step 2", which makes the two look linked, but they are not on this point, because fan-out changes who reads 157 surfaces rather than whether the read is bounded to a principled subset, which is why the step-3 preamble's attribution holds, and the fence-touched unbounded-harvest observation in `## Step 2` is not relied on here; reported separately, the defect also appears in an altered form: the emitted `## Decisions` is not the flat per-route section set SPEC §1 describes but nine `### <token family or pattern>` prose sections covering the whole 157-view app, with zero entries in the dated `- ` bullet form the format and the lint require, so a later `designing-consistently` step 2 could not select the entries for the surface it is touching at all, leaving the slice undefined rather than merely unbounded, and the two skills disagree on addressing, since `baseline-designing-consistently.md` `## What I would have committed` shows the writer side appending under `### Stock`, a per-surface heading the extractor never emits, which bears directly on the PLAN's step-7 / step-8 interface requirement that reader and writer agree on the exact heading shape.
- 2026-08-20 — Ruling (PARENT, answering the lane's blocking ask on where a provisional frontmatter token lives): option A, confirmed, with a binding addition — (i) a role WITH a genuine but merely unconfirmed election puts its token in frontmatter (it must, to compile) AND gets a `[provisional]` entry under `### Global` / `#### Sistema` naming the token and WHAT IT BEAT, carrying BOTH counts, since the numbers are what make a later promote-or-demote decidable ("an entry that says only `[provisional] card is 14px` is not enough"); (ii) a role with NO election gets NO token in frontmatter at all, plus an open-question entry recording the role, the competing candidates and their file locations — because `#### Sistema` is already SPEC §3.4's home for "values and scales: tokens, radii, type, modes, reference-surface declaration" and the owner-approved worked example already carried exactly this shape, so A needs no new syntax and leaves lint and generator untouched; a YAML comment (B) would create a second marker location that nothing reads, and holding unconfirmed tokens out of frontmatter entirely (C) would compile a near-empty `design.tokens.css` and force every build back to raw values, which is the failure `designing-consistently` step 3 exists to stop. The parent's words on the addition: "Marking something provisional does not license inventing it" — emitting `sheet: 20px` for a role the run explicitly declined to elect "is the contamination this lane exists to fix, arriving through the back door." SPEC §4 is unchanged; requirement 5 stands exactly as approved. Full ruling text quoted in PROGRESS.md under `## Reviews`.
- 2026-08-20 — Ruling (PARENT, same reply): SPEC §5 prediction 2 is recorded CONFIRMED — the parent's words: "this baseline result CONFIRMS SPEC section 5's second prediction (that election runs on frequency and lands unconfirmed values as law). Record it as confirmed with this evidence rather than leaving it open" — superseding step 4's own CONTRADICTED-on-the-literal-wording verdict, which is retained below it as the evidence detail rather than as a competing verdict, because the two differ only in what they take prediction 2 to assert: step 4 read it as SPEC §5's literal sentence ("step 4 elects `13px` a token on frequency"), and the transcript does show `13px` was never elected; the parent reads it as the defect that sentence was reaching for, and the same transcript shows that defect present — `radius: card: '14px'` elected on a 984-vs-~751 count the run itself flagged as a near-tie needing the owner's eyes, and `radius: sheet: '20px'` shipped as law for a role the run explicitly declined to elect. Both facts stay in the record; the parent's verdict governs.
- 2026-08-20 — Ruling (step 4 fix round 1): the §3.5 SPEC note now reads "does not resolve at any plausible sibling location searched (controller addendum 4; SPEC §1)" in place of "does not exist on this machine" — because addendum 4 verifies only a depth-2 search of the machine's work-repos folder plus three named candidate paths, not a whole machine, and that overstatement was sitting inside approved SPEC text with prediction 1's de-confounding resting on it; the conclusion survives on the narrower fact, which is also the form the skill's pointer actually takes (SPEC §1 already records that `../Context-Engineering` does not resolve, verified), and ruling 1 above carries the same overstatement but is append-only history, corrected here rather than rewritten.
- 2026-08-20 — Ruling (step 4 fix round 1): name and dismiss prediction 1's fourth candidate cause, the runner's instruction to "proceed through steps 2–5 anyway" (`baseline-designing-consistently.md` `## Step 1`), which is distinct from the scope fence, READ-ONLY and the absent owner because an offer is a halt-and-ask act; the clause is added to the §3.5 SPEC note and the CONTRADICTED verdict stands — because the step was chartered to resolve prediction 1's entanglement, so a fourth cause left unnamed is a hole in that charter, while the verdict survives it on three grounds: the runner attributes the non-offer to its own two causes and never to the proceed instruction, the counterfactual convention it used four times was precisely the tool for recording a would-be offer under such an instruction, and the ruling's load-bearing half, that step 1 produced no discovery either, does not depend on it.
- 2026-08-20 — Ruling (step 4 fix round 1): rewrite the §3.6 SPEC note to record SPEC §5 prediction 2 CONFIRMED on the parent's authority, to keep step 4's literal finding (13px never elected; the frontmatter's `small: '0.8125rem'` is the doc47 `text-small` role, not the 92-count drift value) as evidence beneath that verdict rather than as a competing one, and to close the open question the note used to pose by carrying the parent's two emission rules for steps 6 and 8 — because the parent's ruling above decides where a provisional frontmatter token lives and what may be emitted without one, so leaving "open for the controller, not decided here" inside approved SPEC text would send steps 6 and 8 to a question that is now answered; ruling 2 above stays exactly as written, append-only.
- 2026-08-20 — Ruling (controller, work-run): from step 5 onward, package every review and re-review diff as the full range from the step's base commit to HEAD, naming which commits the range spans, instead of a single named commit's diff — because step 4's fix round spanned two commits across two seats (the first went unresponsive mid-round and a fresh seat picked it up), the re-reviewer was handed only the first commit's diff, and it caught the mismatch itself by checking the live file; a reviewer that had trusted the packet as given would have certified a version of SPEC §3.6 that was no longer what the repo held.
- 2026-08-20 — Ruling (controller, work-run, answering step 4's flagged item 1): SPEC §4 requirement 1's second clause ("does not propose instantiating a DESIGN.md while a system is discoverable") is NOT where the discovery eval gets its RED — the eval takes its RED from the FIRST clause, that step 1 discovers the standing system ranked by SPEC §3.2's source trust, which the cold baseline proves fails today; the second clause is a regression guard on the rewrite and its checklist box may pass trivially against the unfixed skill — because step 4 correctly observed that today's skill reaches no proposal at all, so an eval written only to the negative clause would pass against the pre-rewrite skill and ship a fake RED into step 5, and this repo's hard constraint makes the eval the failing test. Every eval written at steps 5-6 must have at least one expected-behavior box that demonstrably FAILS against the skill as it stands today, and the eval author states which box that is.
- 2026-08-20 — Ruling (controller, work-run): eval FIXTURES stay runtime-neutral — an abstract large multi-module admin with the baseline's real shape and numbers, never a framework, product, or filename from any one repo — while a `## Validation log` entry MAY name the real repo a run was validated against — because `skills/` is a shipped surface the `machine-path` check scans and the fixture is the test's contract, but `skills/extracting-design-md/evals/eval-04.md` already carries a dated validation-log entry naming a real repo and PR, so that precedent is the repo's own and is preserved rather than broken.
- 2026-08-20 — Correction (PARENT, unprompted, diagnosed from this session's transcript): heartbeats move OUT of the wait loop — beat on phase transitions and at most once per ~10 minutes inside a long phase — and every wait on a subagent's verdict file gets a BOUNDED deadline plus a defined action when it expires (record the rung NOT RUN with the reason and move on, as the brief already requires when a runtime refuses a subagent) — because the controller was spin-polling for a verdict file every ~2 seconds and beating on each iteration, emitting roughly 6 beats per minute into the run mailbox, "that is where a real escalation gets buried"; and because a poll loop with no exit "is not a gate, it is a hang waiting to happen" — if the reviewer seat dies and never writes the file, nothing in the loop would say so. Adopted from step 5 onward: waits use a single bounded background watcher that exits on the file appearing or on its deadline, never a per-iteration poll.
- 2026-08-20 — Ruling (controller, work-run, answering step 5's flagged item): `skills/designing-consistently/evals/eval-03.md` box 2 is rewritten to SPEC §3.3's `### <module>` / `#### <route> — <page>` architecture as part of step 5's fix round, NOT left for step 7 — because the box hard-codes the flat `### catalogo/inventario` heading that §3.3 replaces, so a step-7 rewrite built correctly to the approved design would FAIL it; an eval a correct rewrite fails is a broken eval, and this repo's hard constraint puts eval changes before skill content, which means it has to be fixed now rather than discovered as a false failure at step 7. The implementer was right to flag rather than rewrite a file its step was not scoped to touch — the flag is what made the ruling possible.
- 2026-08-20 — Correction (controller, work-run): the controller never again stages with `git add -A` while an implementer holds the working tree; it stages the explicit paths its own commit is about — because at step 5 a controller bookkeeping commit (`eda82ca`, "record the step-5 review verdict verbatim") swept the fix round's in-flight edits to four eval files in with it, so that commit's message describes only the verdict record while its diff also carries another agent's work. Nothing was lost and the history is NOT rewritten (the branch is shared); the mis-attribution is recorded here and in PROGRESS instead, and the implementer was told immediately so it would not read its own clean tree as a failed edit and redo the work.
- 2026-08-20 — Ruling (controller, work-run, step 6 flag 1): `skills/extracting-design-md/evals/eval-02.md` box 1 gains the `[provisional]` requirement in step 6's fix round — because as written it says the consistently-evidenced back button "becomes a dated `## Decisions` entry under its surfaces, citing the evidence" with no marker required, so a step-8 rewrite emitting that entry CONFIRMED passes it; under SPEC §3.2 a pattern evidenced only in non-reference code is a candidate, so the eval has stopped testing SPEC §4 requirement 2 ("nothing discovered is emitted confirmed"), which is the lane's central claim and F02's contract. Not false, but no longer a test of what matters.
- 2026-08-20 — Ruling (controller, work-run, step 6 flag 2): `skills/extracting-design-md/evals/eval-03.md` box 2's flat `### catalogo/inventario` heading is fixed in step 6's fix round while its stale `Context-Engineering` / `context-lint` strings stay for PLAN step 9 — the same split the equivalent `designing-consistently` file already got — because the heading is an architecture change SPEC §3.3 forces and a correct step-8 rewrite would fail it, which is judgment work, whereas step 9 is a `[batch]` MECHANICAL sweep whose acceptance is a `git grep` for two stale strings; folding a design change into a mechanical batch would put judgment work behind a grep gate that cannot see it.
- 2026-08-20 — Ruling (controller, work-run, step 6 flag 3): `skills/extracting-design-md/evals/eval-04.md` keeps its `globals.css`, `@theme` and `[data-theme]` fixture wording — deferred to work-verify, not fixed — because `[data-theme]` and `modes:` are vocabulary of the DESIGN.md convention itself (`Agent-Engineering/reference/design-md.md` defines the `modes:` extension with a default selector of `:root[data-theme="<mode>"]`), so they are format vocabulary rather than one repo's identity; `globals.css` and `@theme` are genuinely framework-flavoured and would not be written today, but this file carries a dated GREEN `## Validation log` recording that THIS fixture was validated on real data, and rewriting the fixture would falsify that log. The runtime-neutral-fixtures ruling governs fixtures written from now on; it is not retroactive against a validated eval whose evidence the edit would destroy.
- 2026-08-20 — WITHDRAWN (controller, correcting its own ruling of the same date on step 6 flag 2): the ruling that `skills/extracting-design-md/evals/eval-03.md` box 2 carries a flat `### catalogo/inventario` heading to be fixed in step 6's fix round is withdrawn and nothing was changed under it — because that file contains no such heading and in fact no `###` heading of any kind; the flag described `skills/designing-consistently/evals/eval-03.md`, which step 5's fix round had already corrected, and the controller ruled on the flag before verifying the premise. The stale `context-lint` reference that file DOES carry stays for PLAN step 9's mechanical sweep, as originally scoped.
- 2026-08-20 — PLAN gap (recorded at the parent's instruction so work-verify reads it as a closed gap rather than an unexplained edit outside the plan): no PLAN step owned `skills/designing-consistently/evals/eval-03.md`, whose box hard-coded the flat `### <surface>` Decisions shape SPEC §3.3 replaces — step 9's `[batch]` sweep names only the `extracting-design-md` file of that name, and step 7 is scoped to `SKILL.md`. Absorbed into step 5's fix round under the evals-before-content constraint. The parent confirmed the ruling: "An eval that a CORRECT rewrite would fail is a broken eval … The alternative — shipping a knowingly-broken eval because no PLAN line named it — would have been the plan governing over the constraint, which is backwards." Step 5's acceptance is unaffected: it counts files, and a rewrite is not an addition.
- 2026-08-20 — Ruling (controller, extending step 6 flag 1 after the parent's generalized audit): `skills/extracting-design-md/evals/eval-02.md` box 1 is fixed for BOTH defects in one edit — it gains the `[provisional]` requirement AND moves from the flat "under its surfaces" addressing to SPEC §3.3's module shape — because the audit found the box carries both of the replaced constructs at once, and it is the ONLY surviving instance of the flat-surface class across all eleven evals in both skills; splitting one line's fix across two steps would leave the eval broken in between.
- 2026-08-20 — Ruling (controller, work-run, at step 7): F01's and F03's `verification` strings are STRENGTHENED, not loosened — F01 replaces a grep for the exact sentence 'Discover the standing system' with checks that step 1's heading is discovery-shaped, that all three discovered source classes are named, that instantiating is the last option and that step 1 is not an instantiate step; F03 replaces a `Global.*module|module.*Global` same-line regex with checks that all three slice components are named and that reading the whole `## Decisions` section is explicitly refused — because both originals were wording proxies rather than behaviour tests: they FAILED against the step-7 rewrite that implements the requirement correctly, and either could have PASSED a skill that merely contained the phrase without the behaviour, which is the exact defect the step-1 reviewer warned about in a row it did not flag. Verified in both directions before the change: each new string passes against the rewritten skill and fails against the pre-step-7 skill at `40b112d`. Both rows were `not_started`, so no `passing` row regressed; SPEC §4's requirements 1 and 3 are unchanged — only how they are proved.
- 2026-08-20 — Ruling (controller, work-run, at step 9): F08's verification is narrowed to the two skill directories AND strengthened with a positive check that `skills/tracing-root-causes` still cites Context-Engineering — because SPEC §4.8 says the references must be gone "in either skill", while the PLAN's grep also covered `README.md`, whose line 68 reads "the base is owner-original (Context-Engineering salvage)": that is an ATTRIBUTION statement about `tracing-root-causes`, the same historical-provenance class the PLAN's own constraints block protects at `skills/tracing-root-causes/SKILL.md:9`, and this repo treats attribution as load-bearing (NOTICE, MAT-94 evidence links). Deleting it to satisfy a grep would falsify where a skill came from. The added positive check means an over-reaching sweep now FAILS this row instead of passing it. README's two skill rows are still refreshed at step 9; they are verified by reading them, which is what a description refresh actually needs.
- 2026-08-20 — Ruling (controller, work-run, at step 10): F10's ordering check is REPLACED, not relaxed — from "the last commit touching a skill's `evals/` is an ancestor of the last commit touching its `SKILL.md`" to "the COMPLETE eval set already existed in the tree at the commit that last rewrote each `SKILL.md`" — because the ancestor form produced a false failure on correct work: PLAN step 9's `[batch]` sweep touched `extracting-design-md/evals/eval-03.md` after step 8's rewrite, and its entire diff is one line repointing the stale name `context-lint` to `agent-lint`, with no behaviour change; an ancestor test cannot distinguish a tooling repoint from a behaviour change, so it would have the lane either report a violation that did not happen or forbid a mechanical sweep the PLAN itself scheduled after step 8. The replacement tests the constraint as the repo states it — evals in place BEFORE the content is written — and was verified in both directions before adoption: it passes at HEAD (both rewrite commits carried the full 5 and 6), and at step 5's commit `2543242` the extracting-design-md set still held only 4, so a rewrite landing there fails the `-eq 6` clause. The underlying facts were confirmed independently: `git ls-tree` at `0e36fef` shows 5 designing-consistently evals and at `4abaf8f` shows 6 extracting-design-md evals.
