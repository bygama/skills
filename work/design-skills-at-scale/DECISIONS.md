# design-skills-at-scale — decisions

<!-- Append-only: date — choice — why. -->

- 2026-08-20 — Ruling (controller, work-run): this lane runs in a linked git worktree, so `../Agent-Engineering` does not resolve from the lane's repo root. Every acceptance command written as `node ../Agent-Engineering/scripts/agent-lint.mjs .` is executed with the sibling checkout resolved to its real location on this machine: `/c/Briar/repos/mine/Agent-Engineering`. Command semantics are unchanged; only the path is resolved. Why: `agent-lint.mjs:25-31` already declares such an escaping path "correct wherever that path exists outside the repo … and absent elsewhere (a worktree)", and reports it `low` rather than failing. The pre-existing `LOW cmd-drift` finding on `AGENTS.md:15` is therefore expected and is not a lane defect.
- 2026-08-20 — Ruling (controller, work-run): the read-only evidence repo `<PEGASUZ>/Pegasuz-Core/frontend-admin/admin` (PLAN constraints block) resolves on this machine to `/c/Briar/repos/mine/../../repos/work/Pegasuz/Pegasuz-Core/frontend-admin/admin`, i.e. `/c/Briar/repos/work/Pegasuz/Pegasuz-Core/frontend-admin/admin`. Verified present. It is READ-ONLY for the whole lane: not one byte is written there. Machine-anchored paths are permitted in `work/` — `agent-lint.mjs:354` scopes the `machine-path` check to `skills|reference|templates|global|loops` only — but never in `skills/`.
